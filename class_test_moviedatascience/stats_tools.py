""" creating a class for code challenge """

import numpy as np
import math as mt

class Stats(array):
    """creating stats class"""
    def __init__(self, array):
        pass

    def mean():
        l = len(numbers)
        mean = sum(array) / l
        print("The mean is: ", mean)

    def standard_deviation():
        std = sum(array) - mean
        squared_mean = std**2
        standardized = mt.sqrt(squared_mean)
        print("The standard deviation is: ", standardized)
